pipeline {
    agent any
    environment {
        SCANNER_HOME = tool 'SonarQube'  // Declare the SonarQube Scanner tool
    }
    stages {
        stage('Checkout') {
            steps {
                // Get code from the Git repository and specify the branch
                git branch: 'wissal', credentialsId: 'GitHub-Credentials', url: 'https://github.com/dhaouzeineb/tp-foyer.git'
            }
        }
        stage('Build') {
            steps {
                // Command to clean the project
                sh 'mvn clean'
            }
        }
        stage('Compile') {
            steps {
                // Command to compile the project
                sh 'mvn compile'
            }
        }
        stage('SonarQube Analysis') {
            steps {
                // Configure SonarQube analysis
                withSonarQubeEnv('SonarQubeToken') {
                    sh """
                        $SCANNER_HOME/bin/sonar-scanner \
                        -Dsonar.projectKey=tp-foyer-project \
                        -Dsonar.projectName=tp-foyer \
                        -Dsonar.sources=src \
                        -Dsonar.java.binaries=target/classes
                    """
                }
            }
        }
          stage('nexus') {
            steps {
                echo 'Deploy to nexus';   
                sh 'mvn deploy -DskipTests -DaltDeploymentRepository=deploymentRepo::default::http://192.168.33.10:8081/repository/maven-snapshots/' 

            }
        }
    }
    post {
        always {
            echo 'Pipeline completed, cleaning up...'
        }
        success {
            echo 'Pipeline executed successfully!'
        }
        failure {
            echo 'Pipeline failed.'
        }
    }
}
